{
  "name": "singr-backend",
  "private": true,
  "version": "0.0.1",
  "description": "Singr centralized backend monorepo",
  "scripts": {
    "build": "npm run build -ws",
    "lint": "npm run lint -ws",
    "test": "npm run test -ws"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "5.5.4"
  }
}
